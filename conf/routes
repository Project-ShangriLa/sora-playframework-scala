# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                            controllers.Application.index
GET        /test                        controllers.Application.test
GET        /testjson                    controllers.Application.testjson

#Anime API V1
## master
GET        /anime/v1/master/cours              controllers.AnimeV1.masterList
GET        /anime/v1/master/:year_num          controllers.AnimeV1.year(year_num)
GET        /anime/v1/master/:year_num/:cours   controllers.AnimeV1.yearCours(year_num, cours)

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file                controllers.Assets.at(path="/public", file)
